
/* hello world. */
{
   "foo":"bar",
   "include":"$[include.json]",
   "baz":"this should be bar: ${foo}",
   
   "nested":{
	   "key":"value",
	   "replace":"the value of foo is ${foo}"
   },
   
   "list":[{
      "test":"test"}],
   "mixed.list":[{"object":"value of foo is ${foo}"},
        "String, value of foo is ${foo}"]
}
// goodbye world
