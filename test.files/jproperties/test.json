/* hello world. */
{
   "foo":"bar",
   "include":"$[include.json]",
   "baz":"this should be bar: ${foo}",
   
   "nested":{
	   "key":"value",
	   "replace":"the value of foo is ${foo}"
   },
   "simple.list":["one", "two", "three", "four", "get your bootie on the floor."],

   "list":[{
      "test":"test"}],
   "mixed.list":[{"object":"value of foo is ${foo}"},
        "String, value of foo is ${foo}"],
        
   "unresolved":"This will not resolve ${lkjljad}"
}
// goodbye world
